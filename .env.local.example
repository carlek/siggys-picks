GEMINI_API_KEY="your gemini key"

NEXT_PUBLIC_FIREBASE_API_KEY="..."
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="..."
NEXT_PUBLIC_FIREBASE_PROJECT_ID="..."
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="..."
NEXT_PUBLIC_FIREBASE_MESSAGE_SENDER_ID="..."
NEXT_PUBLIC_FIREBASE_APP_ID="..."

# Siggy AI prompts (multi-line, wrap in double quotes)
# In production these are injected via Google Secret Manager.
# Locally, if unset, the code falls back to reading from src/ai/prompts/*.txt files.
SIGGY_RECAP_PROMPT="your recap prompt text"
SIGGY_PREVIEW_PROMPT="your preview prompt text"
